package src.classwork;

public class Multiplication2 {
    public static void main(String[] args) {
        for (int row = 1; row <= 12; row++) {
            for (int multiply = 2; multiply <= 2; multiply++) {
                int total = multiply * row;
                System.out.print(multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 3; multiply <= 3; multiply++) {
                int total = multiply * row;
                System.out.print("  " + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 4; multiply <= 4; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 5; multiply <= 5; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 6; multiply <= 6; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 7; multiply <= 7; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 8; multiply <= 8; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 9; multiply <= 9; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 10; multiply <= 10; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 11; multiply <= 11; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 12; multiply <= 12; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 13; multiply <= 13; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 14; multiply <= 14; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 15; multiply <= 15; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 16; multiply <= 16; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 17; multiply <= 17; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 18; multiply <= 18; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 19; multiply <= 19; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 20; multiply <= 20; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 21; multiply <= 21; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 22; multiply <= 22; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 23; multiply <= 23; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            for (int multiply = 24; multiply <= 24; multiply++) {
                int total = multiply * row;
                System.out.print("\t" + " " + multiply + "*" + row + "=" + " " + total);
            }
            System.out.println();
        }

    }
}